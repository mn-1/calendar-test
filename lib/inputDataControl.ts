export type RegisterScheduleDataInfo = {
  title: string;
  memo: string;
  operatorName: string;
  avatar: string;
};

export type EditScheduleDataInfo = {
  title: string;
  memo: string;
  operatorName: string;
  avatar: string;
};
